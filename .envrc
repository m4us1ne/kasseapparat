use flake

# Deliberately not loading the repo-root .env here: it holds production
# secrets for the deployed instance, not development defaults.
# Backend dev config lives in backend/.env (see backend/.env.example).
